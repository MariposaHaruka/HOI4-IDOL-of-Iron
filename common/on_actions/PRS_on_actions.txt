on_actions = {
	on_startup = {
		#스타팅 정책
		effect = {
			PRS = {
				add_ideas = {
					chairman_guidence
					no_parliament
					no_election
					unitary_state
					judiciary_not_working
					
					censored_press
					racial_hierarchy
					patriarchal_society
					no_local_autonomy
					secondary_education
					basic_state_welfare

					planned_economy
					low_economic_mobilisation
					standard_taxation
					limited_exports
					high_military_budget
					support_infrastructure_construction

					separated_armed_force_command
					army_dominant
					push_system
					basic_training
					service_prohibited
					major_officer_training

					volunteer_only
					non_core_recruitment_prohibited
					extraction_programs
					industry_manufacturing_base
					state_controlled_unions_allowed
					graduate_research				
				}
			}
		}
	}
	#on_ruling_party_change_immediate = {
		#의회
	#	effect = {
	#		PRS = { 
	#			if = {
	#				limit = { is_democratic = yes}
	#				parliament_gui_initial_PRS = yes
	#				clr_country_flag = parliament_gui_on
	#				calc_parliament_support_PRS = yes
	#			}
	#		}
    #    }
    #}
	#on_weekly = {
		#의회 지지도 계산
	#	effect = {
	#		PRS = {
	#			if = {
	#				limit = { PRS = { NOT = { has_idea = no_parliament } } }
	#				calc_parliament_support_PRS = yes
	#			}
	#		}
	#	}
	#}
	on_war = {
		effect = {
			remove_ideas = PRS_rearmament_plan
		}
	}
	on_state_control_changed = {
		effect = { #Check how many occupied states Germany has
			if = {
				limit = {
					ROOT = { original_tag = PRS }
					FROM.FROM = {
						NOT = {	is_core_of = ROOT }
						is_on_continent = europe
					}
				}
				add_to_variable = { PRS_occupied_states_europe = 1 }
			}
			if = {
				limit = {
					FROM = { original_tag = PRS }
					FROM.FROM = {
						NOT = {	is_core_of = FROM }
						is_on_continent = europe
					}
				}
				subtract_from_variable = { PRS_occupied_states_europe = 1 }
			}
		}
	}

	on_capitulation_immediate = {
		effect = {
			if = {
				limit = {
					FROM = { original_tag = PRS }
					ROOT = { is_major = yes }
				}
				FROM = { add_to_variable = { GER_capitulated_major_countries = 1 } }
			}
		}
	}

	on_capitulation = {
		effect = {
			if = {
				limit = {
					FROM = {
						OR = {
							original_tag = PRS
							is_ally_with = PRS
						}
					}
					ROOT = {
						OR = {
							capital_scope = { is_on_continent = europe }
							original_tag = GUP
						}
					}
				}
				ROOT = {
					set_country_flag = PRS_has_capitulated_to_projectsekai
					add_to_array = { 
						array = PRS.PRS_seize_gold_targets_array 
						value = THIS
					}
				}
			}
		}
	}
}