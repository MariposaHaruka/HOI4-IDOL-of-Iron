add_core_category = {
	add_core_claim_state = {
		icon = eng_trade_unions_support	
		cost = 0
		state_target = yes
		on_map_mode = map_and_decisions_view
		ai_will_do = {
			base = 10
		}
		
		available = {
			ROOT = { 
				NOT = { has_idea = integration_efforts }
			}
			FROM = { 
				is_owned_by = ROOT
				NOT = { is_core_of = ROOT }
				is_claimed_by = ROOT
				compliance > 79
			}
		}
        visible = {
			FROM = {
				is_owned_by = ROOT
				NOT = { is_core_of = ROOT }
				is_claimed_by = ROOT
				compliance > 59
			}
        }

		complete_effect = {
			FROM = { add_core_of = ROOT }
			ROOT = {
				add_timed_idea = {
					idea = integration_efforts
					days = 120
				}
			}
		}
	}
	add_core_democratic_action = {
		icon = eng_trade_unions_support	
		cost = 50
		state_target = yes
		on_map_mode = map_and_decisions_view
		ai_will_do = {
			base = 0
			modifier = {
				add = 10
				OR = {
					has_stability > 0.8
					conservatism > 0.8
					liberalism_mod > 0.8
					progressivism > 0.8
					socialdemocracy > 0.8
				}
			}
		}
		allowed = {
			FROM = {
				NOT = { has_state_flag = BLA_kivotos_state_flag }
				NOT = { has_state_flag = BLA_gehenna_state_flag }
				NOT = { has_state_flag = BLA_abydos_state_flag }
				NOT = { has_state_flag = BLA_redwinter_state_flag }
				NOT = { has_state_flag = BLA_wildhunt_state_flag }
				NOT = { has_state_flag = BLA_trinity_state_flag }
				NOT = { has_state_flag = BLA_millenium_state_flag }
				NOT = { has_state_flag = BLA_arius_state_flag }
				NOT = { has_state_flag = BLA_hyakkiyako_state_flag }
				NOT = { has_state_flag = BLA_shanhaijing_state_flag }
				NOT = { has_state_flag = BLA_highland_state_flag }
				NOT = { has_state_flag = BLA_valkyrie_state_flag }
				NOT = { has_state_flag = BLA_srt_state_flag }
				NOT = { has_state_flag = BLA_odyssey_state_flag }
			}
		}
		
		available = {
			ROOT = { 
				is_democratic = yes 
				NOT = { has_idea = integration_efforts }
			}
			FROM = { 
				is_owned_by = ROOT
				NOT = { is_core_of = ROOT }
				compliance > 79
			}
		}
        visible = {
			ROOT = { is_democratic = yes }
			FROM = {
				is_owned_by = ROOT
				NOT = { is_core_of = ROOT }
				compliance > 59
			}
        }
		days_remove = 30

		complete_effect = {
			FROM = { add_core_of = ROOT }
			ROOT = {
				add_timed_idea = {
					idea = integration_efforts
					days = 120
				}
			}
		}
	}
}

